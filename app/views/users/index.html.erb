<h1>ユーザー一覧</h1>

<div id="users">
  <% @users.each do |user| %>
    <div class="border rounded-lg bg-gray-500">
      <%= render user %>

      <p>
        <%= link_to "#{user.name}の詳細", user_path(user) %>
      </p>
    </div>
  <% end %>
</div>

<%= paginate @users %>
