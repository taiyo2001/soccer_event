<!DOCTYPE html>
<html>
  <head>
    <title>SoccerEvent</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_importmap_tags %>
    <script src="https://kit.fontawesome.com/4aa12ef06a.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag 'tailwind', 'inter-font', "data-turbo-track": 'reload' %>
    <%= stylesheet_link_tag 'application', "data-turbo-track": 'reload' %>
  </head>
  <body>
    <%= render partial: 'layouts/header', locals: { q: @q } %>
    <main class="pt-10 bg-blue-50">
      <% if notice %>
        <p class="text-green-500 text-center mb-3"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="text-red-500 text-center mb-3"><%= alert %></p>
      <% end %>
      <div class="flex justify-center">
        <div class="max-w-6xl w-full px-4">
          <%= yield %>
        </div>
      </div>
    </main>
    <%= render 'layouts/footer' %>
  </body>
</html>
